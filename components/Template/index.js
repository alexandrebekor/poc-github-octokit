import React from 'react'

const Template = ({ children }) => {
    return (
        <div>
            <main>
                {children}
            </main>
        </div>
    )
}

export default Template